import React, { ReactNode } from 'react';

interface InputFieldProps{
    children: ReactNode;
    
}

const InputField = () => {
    return (
        <div>
            
        </div>
    );
}

export default InputField;
